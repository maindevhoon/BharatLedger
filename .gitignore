# Python
.venv/
__pycache__/
*.pyc

# Env / secrets
.env

# Data (generated, not source-controlled — regenerate via pipeline scripts)
# Note: data/bharat_ledger.sqlite IS tracked (small, required for the deployed API to serve data)
data/synthetic/*.csv
data/synthetic/*.parquet
# Real government PDFs ARE tracked (small, and the provenance for the flagship project)

# Node / frontend
web/node_modules/
web/dist/

# OS
.DS_Store

# Internal planning/process notes — not part of the public repo
agent.md
PLAN.md
pitch-deck-content.md
docs/*.md
Prototype Submission Deck*.pdf
